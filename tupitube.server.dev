#!/bin/bash
BASE=`dirname $0`

# Load the shared environment
source "${BASE}/tupitube.env"

if [ "$1" == "-g" ]
then
    shift 1
    gdb -ex run -ex bt --batch --args ${BASE}/bin/tupitube.server.bin "$*"
else
    ${BASE}/bin/tupitube.server.bin $*
fi
